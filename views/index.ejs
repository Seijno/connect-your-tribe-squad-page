<%- include('./partials/head') %>

<h2>Students</h2>
<ul>
  <% persons.forEach(person => { %>
    <li class="nameLi"><a href="./person/<%= person.id %>"><%= person.name %></a>, squad_id <%= person.squad_id %></li>
  <% }) %>
</ul>

<%- include('./partials/foot') %>